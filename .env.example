# Vercel Environment Variables
# Set these in Vercel Dashboard → Settings → Environment Variables

# Access key hash (SHA256 of your key, first 24 chars)
# Generate: python3 -c "import hashlib; print(hashlib.sha256(b'YOUR_KEY').hexdigest()[:24])"
ACCESS_KEY_HASH=
